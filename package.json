{
  "name": "github-custom-streak-stats",
  "version": "1.0.0",
  "description": "Custom GitHub streak statistics API",
  "main": "api/streak.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "vercel": "^28.0.0"
  }
}